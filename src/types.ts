export interface ImageType {
  id: string;
    small: string;
    regular: string;

  alt_description?: string;
}

export interface ModalImage {
    regular: string,
    alt_description?: string
  }
  